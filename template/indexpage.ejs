<%- include('header', {site: site, title: site.title}); %>
<div class="h-feed">
<% entries.forEach(function (e) { %>
    <%- include('streamentry', {entry: e, utils: utils}); %>
<% }); %>

    <ul class="pager">
        <li><a href="<%= utils.getUrlForIndex(page + 1) %>">Older</a></li>
    </ul>

</div>
<%- include('footer', {site: site}); %>