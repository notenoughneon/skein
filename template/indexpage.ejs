<%- include('header', {title: site.title}); %>
<div class="h-feed">
<% entries.forEach(function (e) { %>
    <%- include('streamentry', {entry: e}); %>
<% }); %>

    <ul class="pager">
        <% if (page < totalPages) { %>
        <li><a href="<%= utils.getPathForIndex(page + 1) %>">Older</a></li>
        <% } %>
    </ul>

</div>
<%- include('footer'); %>