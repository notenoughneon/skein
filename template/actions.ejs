        <div class="blog-post-bottom">
            <span class="blog-post-syndication">
<% entry.syndication.forEach( function(s) {
    var icon;
    if (s.indexOf('twitter.com') !== -1)
        icon = 'fa-twitter';
    else if (s.indexOf('facebook.com') !== -1)
        icon = 'fa-facebook';
    else if (s.indexOf('instagram.com') !== -1)
        icon = 'fa-instagram';
    else
        icon = 'fa-external-link';
%>
                <a class="u-syndication" href="<%= s %>"><i class="fa <%= icon %>"></i></a>
<% }); %>
            </span>
        </div>

